[package]
name = "vector-router-verify"
version = "0.1.0"
edition = "2021"
publish = false
license = "Apache-2.0"
description = "Verification crate for the vector-router samples — compiles, tests and lints the public modules under samples/."

[lib]
path = "src/lib.rs"

[dependencies]
bytemuck = "1"
crossbeam-queue = "0.3"
metrics = "0.24"
